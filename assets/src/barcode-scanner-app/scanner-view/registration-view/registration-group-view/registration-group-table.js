/**
 * External imports
 */
import { Component } from 'react';

export default class RegistrationGroupTable extends Component {
	render() {
		/**
		 * Uses accessible-react-table
		 * Checkin-status column will use a custom component (basically dashicon)
		 * for indicating the check-in status on a registration.
		 * Action column will use buttons
		 */
	}
}
